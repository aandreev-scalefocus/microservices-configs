ribbon:
  serverListRefreshInterval: 1000
  ConnectTimeout: 6000
  ReadTimeout: 3000

hystrix.command.default.execution.isolation:
    thread.timeoutInMilliseconds: 10000
    strategy: THREAD

feign.hystrix.enabled: true

zuul.routes.employees.path: /employees/** 
zuul.routes.employees.service-id: rest-server

rest-server.name: rest-server3